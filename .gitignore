*.tgz
charts/**/charts/
charts/**/Chart.lock
.DS_Store

# private, per-operator chart deny-list (never commit)
.chart-deny-extra

# git worktrees for in-progress tickets (never commit)
.claude/worktrees/
