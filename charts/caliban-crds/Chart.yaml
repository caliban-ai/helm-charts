apiVersion: v2
name: caliban-crds
description: CustomResourceDefinitions for the caliban-ai operator (Workspace + CalibanTask) — installed as their own step
type: application
version: 0.2.5
appVersion: "0.2.5"   # synced to caliban-operator main 892a2e5 (post-v0.5.0, unreleased): additive — CalibanTask.spec.task.permissionPosture (supervised|unattended), status.permissionPosture + a `Posture` printer column, Workspace.spec.agentPolicy.allowUnattended (#80, operator ADR 0006). Safe to install ahead of the operator: v0.5.0 ignores the new fields, so an `unattended` task is not gated until an operator with #80 runs.
