apiVersion: v2
name: caliban-crds
description: CustomResourceDefinitions for the caliban-ai operator (Workspace + CalibanTask) — installed as their own step
type: application
version: 0.2.4
appVersion: "0.2.4"   # synced to caliban-operator main 60b8f15 (post-v0.4.0, unreleased): additive — Workspace.spec.egress allow-list (#58), CalibanTask.spec.state.tokenRef (#41/#53), status.conditions as a map-list keyed by `type` for server-side apply (#64), doc fixes (#77). Safe to install ahead of the operator: an older operator ignores the new fields.
