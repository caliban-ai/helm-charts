apiVersion: v2
name: caliban-operator
description: caliban-operator (kube-rs) controller — reconciles CalibanTask into sandboxed caliband pods
type: application
version: 0.2.2
appVersion: "0.3.0"   # v0.3.0 = build_sandbox launches caliband with session-plane token + TLS (Bug 2)
