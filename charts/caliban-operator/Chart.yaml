apiVersion: v2
name: caliban-operator
description: caliban-operator (kube-rs) controller — reconciles CalibanTask into sandboxed caliband pods
type: application
version: 0.2.3
appVersion: "0.3.1"   # v0.3.1 = per-agent stream reachable — advertise-host + NetworkPolicy port range (#24, #25)
