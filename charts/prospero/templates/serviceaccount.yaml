{{- if not (has .Values.topology (list "standalone" "clustered")) }}
{{- fail (printf "topology must be 'standalone' or 'clustered', got %q" .Values.topology) }}
{{- end }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "prospero.fullname" . }}
  labels:
    {{- include "prospero.labels" . | nindent 4 }}
