{{- if eq .Values.topology "clustered" }}
{{- if not (or .Values.database.existingSecret .Values.database.url) }}
{{- fail "topology=clustered requires database.existingSecret or database.url" }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "prospero.fullname" . }}
  labels:
    {{- include "prospero.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "prospero.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "prospero.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "prospero.fullname" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
      containers:
        - name: prosperod
          image: {{ include "prospero.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --addr
            - "0.0.0.0:{{ .Values.service.port }}"
            - --host
            - {{ .Values.host | quote }}
            - --lease-ttl-secs
            - {{ .Values.leaseTtlSecs | quote }}
            {{- if not .Values.autostart }}
            - --no-autostart
            {{- end }}
          env:
            - name: PROSPERO_REPLICA_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: PROSPERO_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "prospero.dbSecretName" . }}
                  key: {{ .Values.database.secretKey }}
            {{- range $k, $v := .Values.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
