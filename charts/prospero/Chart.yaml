apiVersion: v2
name: prospero
description: Prospero control plane (prosperod) + dashboard — part of the caliban-ai system
type: application
version: 0.2.1
appVersion: "0.3.0"   # prosperod built --features k8s (needed for fleetBackend=k8s)
