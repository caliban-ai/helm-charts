apiVersion: v2
name: prospero
description: Prospero control plane (prosperod) + dashboard — part of the caliban-ai system
type: application
version: 0.2.3
appVersion: "0.3.2"   # prosperod built --features k8s (needed for fleetBackend=k8s); v0.3.2 = spawn no longer blocks on reconcile (Bug 1)
