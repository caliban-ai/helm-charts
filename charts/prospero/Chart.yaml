apiVersion: v2
name: prospero
description: Prospero control plane (prosperod) + dashboard — part of the caliban-ai system
type: application
version: 0.2.7
appVersion: "0.6.0"   # prosperod built --features k8s (needed for fleetBackend=k8s); v0.6.0 = one dashboard, served at / with its files under /assets/ — BREAKING: the v1 page (#197) and the /v2 alias (#204) are deleted and now 404, so any route or bookmark carrying a version prefix must drop it (this chart matches on host only, so nothing here changes). A terminal pod agent is also resolved once instead of re-dialed every ~2s forever (#170). Still needs caliban-crds >= 0.2.1 or the API server prunes spec.task.interactive
